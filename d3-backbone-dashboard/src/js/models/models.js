var app = app || {};

(function () {
    'use strict';

    // Models
    // ----------

    app.Product = Backbone.Model.extend({}),
    app.Issue = Backbone.Model.extend({});
    app.User = Backbone.Model.extend({});

})();